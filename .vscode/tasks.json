{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "func",
      "command": "host start",
      "problemMatcher": "$func-watch",
      "isBackground": true,
      "options": {
        "cwd": "${workspaceFolder}/functions"
      }
    },
    {
      "type": "shell",
      "label": "build",
      "command": "npm run build",
      "options": {
        "cwd": "${workspaceFolder}/functions"
      }
    }
  ]
}